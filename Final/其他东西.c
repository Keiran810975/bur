#include<stdio.h>
#include<math.h>
#include<string.h>
#include<ctype.h>
#include<stdlib.h>
int main(){
    cbrt()//立方根
    double pi=acos(-1);
    long long c=0xfffffff;//int的最大值
    scanf(" %c",&c)//不用getchar了
    scanf("%[abfg]",s);//读到这些字符之外的字符时终止
    scanf("%[^abfg]",s);//反过来
    scanf("%[^\n]",str);//读入字符串直到换行符停止
    scanf("%[0-9]",s);
    scanf("%[a-zA-Z0-9]",s);//可以连起来
    %5d // 打印五格，不足则左边补空格
    %-5d //打印五格，不足则右边补空格
    %05d //打印五格，不足则左边补 0
    %.5f //保留小数点后五位
    return 0;
}
