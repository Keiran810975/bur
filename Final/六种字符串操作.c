op=1时进行字符串拼接，给出一个非空字符串 str2，将 str2拼接在 str1末尾。
op=2时进行字符串截断，给出一个整数 i，将 str1自第  位以后的内容删去（包括第 i位）。
op=3时进行字符串插入，给出一个整数 i，一个非空字符串 str2，将 str2插入在 str1的第 i位之后。
op=4时进行字符串删除，给出两个整数 i, j，将 str1第 i位到第 j位（包括第 i, j位） 之间的内容删除，可能存在的之后的字符向前补位。
op=5时进行字符串替换，给出两个整数 i, j，一个非空字符串 str2，将 str1第 i位到第 j位（包括第 i, j位） 之间的内容替换为 str2。
op=6时进行字符串查询，给出一个非空字符串 str2。若 str2存在于 str1中，输出 str2在 str1中第一次出现和最后一次出现的位置，用一个空格分开。
若不存在，输出一行 NULL 。


见E7solution  H题